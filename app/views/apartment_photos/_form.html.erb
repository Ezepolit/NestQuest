<%= form_for(@apartment_photo, :html => { :multipart => true }) do |f| %>
  <% if @apartment_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apartment_photo.errors.count, "error") %> prohibited this apartment_photo from being saved:</h2>

      <ul>
      <% @apartment_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :apartment_id %><br>
    <%= f.text_field :apartment_id %>
  </div>
  <%= f.file_field :photos %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
